# inkwell
Inkwell is a CLI-native profiler and VS Code extension that instruments the Stylus TestVM to generate function-level ink reports, identifying "dry nib" bugs where host-call overhead charges for more data than is actually returned. 
